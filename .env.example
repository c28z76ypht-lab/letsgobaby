# Postgres connection (Neon, Supabase, Railway, local Docker, etc.)
# Neon: Project → Connection details → copy the pooled `postgresql://...` string (sslmode=require is typical).
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
